[project]
name = "mygpt"
version = "0.2.0"
description = "Chapter 2: Project setup with `uv`"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
mygpt = "mygpt:main"

[build-system]
requires = ["uv_build>=0.8.0,<0.9"]
build-backend = "uv_build"
